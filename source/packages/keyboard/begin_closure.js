// The whole Keyboard package is built inside a closure to provide private utility
// methods and classes only accessible to the Keyboard package's internals.
(function(KeyListener, Event, doc) {
    var KEYS = KeyListener.KEY;
